name: reboot
description: "A new Flutter project."
publish_to: 'none'

# Добавьте этот блок ОБЯЗАТЕЛЬНО:
environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4 # Для выбора картинок
  http: ^1.2.0
  web: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
